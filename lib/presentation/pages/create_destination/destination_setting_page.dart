import 'package:flutter/material.dart';

class DestinationSettingPage extends StatelessWidget {
  const DestinationSettingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
